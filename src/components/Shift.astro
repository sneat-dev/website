---
---

<section id="the-shift" class="section shift">
  <div class="container-narrow center" data-reveal>
    <p class="eyebrow">The shift</p>
    <h2 class="shift-line">
      AI made writing code cheap.<br />
      It made <span class="grad-text">context</span> the bottleneck.
    </h2>
    <p class="shift-body">
      The hard part was never the typing. It’s keeping everyone — human and
      machine — working from the same truth. Scattered, rotting context is the
      real tax on shipping software. It’s the one problem every tool here is
      built to kill.
    </p>
  </div>
</section>

<style>
  .shift {
    text-align: center;
  }
  .shift-line {
    font-size: clamp(2rem, 4.4vw, 3.4rem);
    margin-bottom: 1.4rem;
  }
  .shift-body {
    font-size: clamp(1.05rem, 1.5vw, 1.25rem);
    line-height: 1.6;
    color: var(--color-text-muted);
    max-width: 58ch;
    margin-inline: auto;
  }
  .center {
    text-align: center;
  }
</style>
